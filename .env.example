# Railway will automatically set PORT
PORT=5000

# Database (Railway PostgreSQL will automatically set this)
DATABASE_URL=postgresql://username:password@hostname:port/database

# Node Environment
NODE_ENV=production